# The compiled pkl IS committed (yolo_tinygrad.pkl): the device gets it via git,
# no on-device compile. Regenerate with compile_yolo_onnx.py when the model changes.
!yolo_tinygrad.pkl
yolo_tinygrad.pkl.chunk*
*.onnx
*.onnx.data
