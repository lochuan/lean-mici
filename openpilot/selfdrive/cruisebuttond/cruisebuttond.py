#!/usr/bin/env python3
"""cruisebuttond stub: shapes cruise set speed via simulated steering-wheel button presses.

Placeholder only — the real daemon replaces this module in a later task
(Task 6). The process gate (CruiseButtonsEnabled) defaults to off, so this
main() is never reached until the user enables the feature.
"""


def main():
  raise RuntimeError("cruisebuttond is a stub; replaced by the real daemon in a later task")